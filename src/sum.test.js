import sum from './sum'
it("when a and b are num", ()=>
{
    const result=sum(1,2)
    expect(result).toBe(3);
    
});
test("my first test ",()=>
{
    let data=10;
    expect(data).toBe(10)
})
